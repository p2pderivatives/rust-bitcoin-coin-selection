[package]
name = "rust-bitcoin-coin-selection"
version = "0.1.0"
authors = ["Crypto Garage"]
edition = "2018"

[features]
rand = ["bitcoin/rand"]

[dependencies]
bitcoin = { version = "*" }

[dev-dependencies]
bitcoin = {version = "*", features = ["rand"]}
